const Header = () => {
    return(
        <div style = {divStyle}>
            <header>   
                <h1>Hi!</h1>
            </header>
        </div>
    )
};

var divStyle = {
    padding: "20px",
};

export default Header;