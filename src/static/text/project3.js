const project3String = 'A fully functional Connect 4 game versus an AI which uses the minimax algorithm. This game is built using python, leveraging the Pygame module for the visuals. The backend logic uses a 2d - numpy array to represent the board state.';

const project3Text = () => {
    return project3String;
};

export default project3Text;